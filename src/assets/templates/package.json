{
  "name": "<APP_NAME>",
  "version": "1.0.0",
  "description": "<APP_DESCRIPTION>",
  "main": "src/index.js",
  "scripts": {
    "dev": "gulp dev",
    "build": "gulp build",
    "deploy": "gulp build && git add . && git commit --amend --no-edit && git push --all"
  },
  "devDependencies": {
    "express": "^4.17.2",
    "gulp": "^4.0.2",
    "gulp-csso": "^4.0.1",
    "gulp-esbuild": "^0.10.0",
    "gulp-replace": "^1.1.3",
    "html-minifier": "^4.0.0"
  },
  "license": "ISC"
}
